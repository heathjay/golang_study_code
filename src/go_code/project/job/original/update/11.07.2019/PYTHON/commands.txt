# start our curated SCONE Python container image
sudo docker run --device=/dev/isgx -it sconecuratedimages/crosscompilers:python27 bash

# install python chess 
pip install python-chess

# start python inside of an enclave and allow dynamic loading of libraries
# and use example from https://pypi.python.org/pypi/python-chess

SCONE_MODE=HW SCONE_ALLOW_DLOPEN=2 SCONE_ALPINE=1 SCONE_VERSION=1 python

import chess
board = chess.Board()
board.legal_moves
chess.Move.from_uci("a8a1") in board.legal_moves
board.push_san("e4")
board.push_san("e5")
board.push_san("Qh5")
board.push_san("Nc6")
board.push_san("Bc4")
board.push_san("Nf6")
board.push_san("Qxf7")
board.is_checkmate()
board

exit()
exit
exit
