

# start our curated SCONE Python container image
sudo docker run --device=/dev/isgx -it -v "$PWD":/usr/src/myapp -w /usr/src/myapp sconecuratedimages/crosscompilers:python27 bash

virtualenv SCONE
source SCONE/bin/activate
pip install -r requirements.txt

SCONE_MODE=HW SCONE_ALLOW_DLOPEN=2 SCONE_ALPINE=1 SCONE_VERSION=1 python app.py

exit()
exit
exit
